import { SxProps, Theme } from '@mui/material/styles';

export const styles: Record<string, SxProps<Theme>> = {
    popover : {
        marginTop: 1,
    },
    colorsContainer: {
        padding: 2,
        display: 'flex',
        flexDirection: 'row',
        width: '144px',
        flexWrap: 'wrap',
    },
};
