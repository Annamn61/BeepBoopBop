import { SxProps, Theme } from '@mui/material/styles';

export const styles: Record<string, SxProps<Theme>> = {
    square: {
        width: '20px',
        height: '20px',
        borderRadius: '4px',
    }
};
