import Box from '@mui/material/Box';
import Typography from '@mui/material/Typography';

const Event = () => {
  return (
    <Box sx={{
    }}>
        <Typography>stay in the know</Typography>
        <Typography variant='h4'>Oregano</Typography>
    </Box>
  );
};

export default Event;