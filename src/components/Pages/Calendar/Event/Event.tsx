import Box from '@mui/material/Box';
import Typography from '@mui/material/Typography';

const Event = () => {
  return (
    <Box sx={{}}>
      <Typography variant="body2">stay in the know</Typography>
      <Typography variant="h1">Oregano</Typography>
    </Box>
  );
};

export default Event;
